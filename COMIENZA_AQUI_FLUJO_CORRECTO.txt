═══════════════════════════════════════════════════════════════════════════════════

                    🎯 COMIENZA AQUÍ - FLUJO CORRECTO 🎯

═══════════════════════════════════════════════════════════════════════════════════

ARQUITECTURA:

Tu Máquina → GitHub → VM Control (ESXi) → bootstrap_complete.yml → 3 VMs → Network Monitor

═══════════════════════════════════════════════════════════════════════════════════

✅ PASO 1: Tu Máquina - PUSH A GITHUB (2 minutos)
────────────────────────────────────────────────

PowerShell en D:\ansible:

    git add -A
    git commit -m "feat: Network Monitor Dashboard + Extended Topology"
    git push origin main

✅ LISTO. Código en GitHub.

═══════════════════════════════════════════════════════════════════════════════════

✅ PASO 2: VM Control (ESXi) - INSTALACIÓN INICIAL (15 minutos)
───────────────────────────────────────────────────────────────

Conectate a la VM Control que ya existe en ESXi:

    sudo apt update && sudo apt upgrade -y

    sudo apt install -y \
      git python3 python3-pip ansible curl wget \
      openssh-client openssh-server net-tools iputils-ping dnsutils

    pip3 install pyvmomi netaddr passlib

✅ LISTO. Dependencias instaladas.

═══════════════════════════════════════════════════════════════════════════════════

✅ PASO 3: VM Control - CLONAR REPOSITORIO (2 minutos)
───────────────────────────────────────────────────────

En VM Control:

    mkdir -p ~/ansible-repo
    cd ~/ansible-repo
    git clone https://github.com/kat14221/ansible.git
    cd ansible

    ls -la

✅ LISTO. Repo clonado.

═══════════════════════════════════════════════════════════════════════════════════

✅ PASO 4: VM Control - CONFIGURAR CREDENCIALES (2 minutos)
──────────────────────────────────────────────────────────

En VM Control, en el directorio ansible:

    cp group_vars/all/vault.yml.template group_vars/all/vault.yml

    nano group_vars/all/vault.yml

    # Edita:
    vault_vcenter_hostname: "172.17.25.1"
    vault_vcenter_username: "root"
    vault_vcenter_password: "TU_CONTRASEÑA_ESXI"
    vault_vcenter_port: 443
    vault_vcenter_validate_certs: false

    # Guarda: Ctrl+O → Enter → Ctrl+X

✅ LISTO. Credenciales configuradas.

═══════════════════════════════════════════════════════════════════════════════════

✅ PASO 5: VM Control - EJECUTAR BOOTSTRAP (20-30 minutos)
───────────────────────────────────────────────────────

En VM Control, en el directorio ansible:

    ansible-playbook playbooks/bootstrap_complete.yml \
      -i inventory/hosts.yml \
      -u root \
      -k

    # Te pedirá contraseña SSH de ESXi (root)

    # ESPERA A QUE TERMINE (~20-30 minutos)

    # Verás:
    ✅ VMs creadas exitosamente
    ✅ debian-router (172.17.25.126)
    ✅ ubuntu-pc (172.17.25.10)
    ✅ windows-pc (172.17.25.11)

✅ LISTO. 3 VMs creadas. Espera a que terminen instalación del SO.

═══════════════════════════════════════════════════════════════════════════════════

✅ PASO 6: ESPERAR (10-20 minutos)
──────────────────────────────────

Las VMs están instalando Debian/Ubuntu/Windows automáticamente.

Verifica en vSphere que estén en estado "Running":
    https://168.121.48.254:10101/ui/#/host/vms

═══════════════════════════════════════════════════════════════════════════════════

✅ PASO 7: debian-router - CLONAR Y DESPLEGAR (5 minutos)
──────────────────────────────────────────────────────────

SSH a debian-router:

    ssh ansible@172.17.25.126

En debian-router:

    cd /home/ansible
    git clone https://github.com/kat14221/ansible.git
    cd ansible

    chmod +x scripts/deploy_and_run.sh
    bash scripts/deploy_and_run.sh

    # ESPERA ~2-3 minutos

    # Verás:
    ╔════════════════════════════════════════════════╗
    ║        🎉 SISTEMA LEVANTADO 🎉               ║
    ╚════════════════════════════════════════════════╝
    
    Network Monitor disponible en:
    http://172.17.25.126:5000

✅ LISTO. Network Monitor activo.

═══════════════════════════════════════════════════════════════════════════════════

✅ PASO 8: ACCEDER AL DASHBOARD
──────────────────────────────────

Abre navegador:

    http://172.17.25.126:5000

Verás:
    • Dashboard con dispositivos detectados
    • Tabla con IPv6, MAC, latencia
    • Botones: Escanear, Buscar, SSH, Ping, Exportar
    • Auto-refresh cada 30 segundos

✅ LISTO. ¡TODO EN ACCIÓN!

═══════════════════════════════════════════════════════════════════════════════════

⏱️  TIEMPO TOTAL: ~50-70 minutos

═══════════════════════════════════════════════════════════════════════════════════

📍 COMANDOS RÁPIDOS PARA COPIAR Y PEGAR

TU MÁQUINA (PowerShell D:\ansible):
────────────────────────────────────
git add -A; git commit -m "feat: Network Monitor Dashboard + Extended Topology"; git push origin main

VM CONTROL - INSTALACIÓN:
──────────────────────────
sudo apt update && sudo apt upgrade -y
sudo apt install -y git python3 python3-pip ansible curl wget openssh-client openssh-server net-tools iputils-ping dnsutils
pip3 install pyvmomi netaddr passlib

VM CONTROL - SETUP:
────────────────────
mkdir -p ~/ansible-repo && cd ~/ansible-repo
git clone https://github.com/kat14221/ansible.git && cd ansible
cp group_vars/all/vault.yml.template group_vars/all/vault.yml
# Editar vault.yml con credenciales ESXi

VM CONTROL - BOOTSTRAP:
────────────────────────
ansible-playbook playbooks/bootstrap_complete.yml -i inventory/hosts.yml -u root -k

DEBIAN-ROUTER - DEPLOY:
────────────────────────
ssh ansible@172.17.25.126
cd /home/ansible && git clone https://github.com/kat14221/ansible.git && cd ansible
chmod +x scripts/deploy_and_run.sh && bash scripts/deploy_and_run.sh

NAVEGADOR:
───────────
http://172.17.25.126:5000

═══════════════════════════════════════════════════════════════════════════════════

✨ LISTO PARA COMENZAR

Próximo paso: Lee README_FLUJO_CORRECTO.md para más detalles

═══════════════════════════════════════════════════════════════════════════════════
