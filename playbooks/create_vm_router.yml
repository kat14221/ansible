---
# Playbook: Crear VM Debian Router en ESXi
# Descripción: Aprovisiona VM Debian como router IPv6
# Red: 2025:DB8:220::1/64 (LAN) + 2025:DB8:101::a1/64 (Uplink)

- name: Crear VM Debian Router
  hosts: vm_router
  gather_facts: no
  connection: local
  tasks:
    - name: Incluir role vmware-router
      include_role:
        name: vmware-router

    - name: Mostrar resumen
      debug:
        msg:
          - "================================================"
          - "  VM Debian Router Creada"
          - "================================================"
          - ""
          - "VM: {{ vm_name }}"
          - "IPv6 LAN: {{ networks[0].ipv6_address }}/{{ networks[0].ipv6_prefix }}"
          - "Port-group: {{ networks[0].name }}"
          - "ISO: {{ cdrom }}"
          - ""
          - "Próximo paso: Instalar SO manualmente en la VM"
          - "================================================"
