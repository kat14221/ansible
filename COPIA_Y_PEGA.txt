═══════════════════════════════════════════════════════════════════════════════
                        COPIAR Y PEGAR EXACTO
═══════════════════════════════════════════════════════════════════════════════

Si no quieres leer documentación, aquí está TODO lo que necesitas ejecutar
en orden exacto. Solo copia y pega cada comando.

═══════════════════════════════════════════════════════════════════════════════

TERMINAL 1: PowerShell en D:\ansible
─────────────────────────────────────

Abre PowerShell, ve a D:\ansible y ejecuta:

    powershell -ExecutionPolicy Bypass -File scripts\push_to_github.ps1

Espera a que termine. Verás:

    ╔═══════════════════════════════════════════════════════════╗
    ║              ✅ PUSH COMPLETADO EXITOSAMENTE ✅         ║
    ╚═══════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════════════

TERMINAL 2: SSH a debian-router
────────────────────────────────

Abre OTRA terminal PowerShell y ejecuta:

    ssh ansible@172.17.25.126

Te pedirá contraseña. Escribe:

    ansible

Presiona Enter.

Verás algo como:

    The authenticity of host '172.17.25.126' can't be established.
    ECDSA key fingerprint is SHA256:...
    Are you sure you want to continue connecting (yes/no)? yes

Escribe: yes

Luego verás:

    ansible@debian-router:~$

═══════════════════════════════════════════════════════════════════════════════

TERMINAL 2: Git pull + Deploy
──────────────────────────────

Aún en debian-router, ejecuta LÍNEA POR LÍNEA:

Línea 1:
    cd /home/ansible/ansible

Línea 2:
    git pull origin main

Espera a que termine. Verás:

    From github.com:kat14221/ansible
       abc1234..def5678  main     -> origin/main
    Updating abc1234..def5678
    Fast-forward
     roles/network-monitor/files/app.py | 320 ++
     ... (más archivos)

Línea 3:
    chmod +x scripts/deploy_and_run.sh

Línea 4:
    bash scripts/deploy_and_run.sh

Ahora ESPERA ~2-3 MINUTOS a que termine.

Verás todo esto:

    ═══════════════════════════════════════════════════════════════
      🚀 DEPLOY AND RUN - Network Monitor + Full Stack
    ═══════════════════════════════════════════════════════════════

    📍 PASO 1: Verificar que estamos en debian-router
    ───────────────────────────────────────────────────────────────
    Hostname actual: debian-router
    ✅ Estamos en debian-router correctamente

    📥 PASO 2: Git Pull del repositorio
    ✅ Git pull completado

    ... (más pasos)

    📊 PASO 8: Información de Acceso
    ╔═══════════════════════════════════════════════════════════╗
    ║              🎉 SISTEMA LEVANTADO 🎉                     ║
    ╚═══════════════════════════════════════════════════════════╝

    📍 Network Monitor Dashboard disponible en:
       • IPv4: http://172.17.25.126:5000
       • IPv6: http://[2025:db8:101::1]:5000

═══════════════════════════════════════════════════════════════════════════════

TERMINAL 3: Abrir navegador
────────────────────────────

Abre tu navegador (Chrome, Firefox, Edge, etc.) y ve a:

    http://172.17.25.126:5000

VERÁS ESTO (Network Monitor Dashboard):

┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  🌐 Network Monitor Dashboard                      🔄 Actualizado hace 5s   │
│                                                                              │
│  ┌──────────────┬──────────────┬──────────────┬──────────────┐              │
│  │ Dispositivos │   Latencia   │   Escaneos   │   Ultimos    │              │
│  │      6       │   15.2 ms    │     42       │  Activos     │              │
│  └──────────────┴──────────────┴──────────────┴──────────────┘              │
│                                                                              │
│  [Buscar dispositivos...]  [Escanear Red]  [📊 Exportar]  [🔄 Auto-Refresh]│
│                                                                              │
│  Dispositivos en la Red:                                                    │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │ Hostname      │ IPv6                  │ MAC          │ Latencia │        │
│  ├─────────────────────────────────────────────────────────────────┤        │
│  │ debian-router │ 2025:db8:101::1      │ 52:54:00:12:34:56 │ 0.5 ms  │  │
│  │ ubuntu-pc     │ 2025:db8:101::10     │ 52:54:00:ab:cd:ef │ 2.1 ms  │  │
│  │ windows-pc    │ 2025:db8:101::11     │ 52:54:00:fe:dc:ba │ 3.4 ms  │  │
│  │ physical-rtr  │ 2025:db8:101::2      │ 00:1a:2b:3c:4d:5e │ 5.2 ms  │  │
│  └─────────────────────────────────────────────────────────────────┘        │
│                                                                              │
│  [Detalles] [🖥️ Terminal SSH] [📌 Ping]                                    │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════

PRUEBAS RÁPIDAS EN EL DASHBOARD
────────────────────────────────

Haz clic en estos botones para probar:

1. [Escanear Red]
   → Detecta dispositivos
   → Espera ~10 segundos
   → Tabla se actualiza

2. Escribe en [Buscar dispositivos...]
   → Por ejemplo: "ubuntu"
   → Tabla filtra automáticamente

3. [🖥️ Terminal SSH] (en cualquier dispositivo)
   → Muestra comando SSH
   → Haz clic para copiar

4. [📌 Ping]
   → Verifica latencia

5. [📊 Exportar]
   → Descarga JSON o CSV

6. [🔄 Auto-Refresh]
   → Actualiza cada 30 segundos

═══════════════════════════════════════════════════════════════════════════════

¿ALGO FALLA? Troubleshooting rápido
───────────────────────────────────

En Terminal 2 (debian-router), ejecuta:

A) Ver logs:
   tail -f /var/log/network-monitor/app.log

B) Ver estado del servicio:
   systemctl status network-monitor

C) Reiniciar:
   sudo systemctl restart network-monitor
   sleep 2
   curl http://localhost:5000/api/devices

D) Ver archivos:
   ls -lh /opt/network-monitor/

═══════════════════════════════════════════════════════════════════════════════

RESUMEN FINAL
─────────────

✅ Hiciste push a GitHub
✅ Hiciste pull en debian-router
✅ Ejecutaste script de deploy (~3 minutos)
✅ Network Monitor está corriendo
✅ Ves dashboard en http://172.17.25.126:5000

🎉 ¡LISTO! Tienes monitoreo de red en acción.

═══════════════════════════════════════════════════════════════════════════════

¿QUÉ SIGUE?
───────────

Opcional (ver docs/TOPOLOGIA_EXTENDIDA.md):
  • Expandir con GNS3 en laptop externa
  • Conectar Access Point WiFi
  • Monitorear 15 dispositivos en vez de 6

Documentación disponible:
  • roles/network-monitor/README.md - Guía completa
  • docs/TOPOLOGIA_EXTENDIDA.md - Expansión futura
  • INDICE_FINAL.md - Índice del proyecto

═══════════════════════════════════════════════════════════════════════════════

COMANDOS MÁS IMPORTANTES (referencia rápida)
──────────────────────────────────────────────

Desde debian-router:

Ver dashboard:     http://172.17.25.126:5000
Probar API:        curl http://localhost:5000/api/devices
Ver logs:          tail -f /var/log/network-monitor/app.log
Reiniciar:         sudo systemctl restart network-monitor
Estado:            systemctl status network-monitor

═══════════════════════════════════════════════════════════════════════════════

¡VAMOS! 🚀

Comienza con el TERMINAL 1 (PowerShell push_to_github.ps1)

═══════════════════════════════════════════════════════════════════════════════
