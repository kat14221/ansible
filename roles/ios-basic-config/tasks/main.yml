---
# Role: ios-basic-config
# Descripción: Configuración TEMPORAL de routers Cisco IOS con IPv6
# IMPORTANTE: NO guarda cambios permanentes (router académico compartido)

- name: Verificar configuración actual (sin guardar)
  debug:
    msg: "Aplicando configuración temporal - NO se guardarán cambios permanentes"

- name: Configurar hostname (temporal)
  cisco.ios.ios_config:
    lines:
      - "hostname {{ hostname }}"
    save_when: never
  when: hostname is defined

- name: Configurar banner (temporal)
  cisco.ios.ios_config:
    lines:
      - "banner motd # Red Academica IPv6 - VMWARE-101001 - TEMPORAL #"
    save_when: never

- name: Configurar credenciales de usuario (temporal)
  cisco.ios.ios_config:
    lines:
      - username cisco privilege 15 secret cisco123
      - username ansible privilege 15 secret Ansible123!
    save_when: never

- name: Configurar línea de consola (temporal)
  cisco.ios.ios_config:
    lines:
      - login local
    parents: line console 0
    save_when: never

- name: Configurar líneas VTY (temporal)
  cisco.ios.ios_config:
    lines:
      - login local
      - transport input ssh
    parents: line vty 0 4
    save_when: never

- name: Configurar dominio (omitido - ya configurado)
  debug:
    msg: "Dominio ya configurado en el router"

- name: Generar llaves RSA para SSH (omitido - ya configurado)
  debug:
    msg: "SSH ya configurado en el router"

- name: Habilitar IPv6 unicast routing (temporal)
  cisco.ios.ios_config:
    lines:
      - ipv6 unicast-routing
    save_when: never
  ignore_errors: yes

- name: Configurar interfaces de router (temporal)
  cisco.ios.ios_config:
    lines:
      - "description {{ item.value.description }}"
      - "ipv6 address {{ item.value.ipv6_address }}/{{ item.value.ipv6_prefix }}"
      - ipv6 enable
      - no shutdown
    parents: "interface {{ item.key }}"
    save_when: never
  loop: "{{ physical_router_interfaces | dict2items }}"
  when: physical_router_interfaces is defined

- name: Configuración de switch omitida (solo para routers)
  debug:
    msg: "Configuración de switch no aplicable para router físico"
  when: inventory_hostname == "physical-router"

- name: Configuración NO guardada (router académico compartido)
  debug:
    msg: "IMPORTANTE: Configuración temporal aplicada - NO se guardan cambios permanentes"

- name: Obtener configuración running
  cisco.ios.ios_command:
    commands: show running-config
  register: running_config

- name: Guardar running-config
  copy:
    content: "{{ running_config.stdout[0] }}"
    dest: "evidence/configs/{{ inventory_hostname }}_running_config.txt"
  delegate_to: localhost

- name: Obtener información de interfaces IPv6
  cisco.ios.ios_command:
    commands:
      - show ipv6 interface brief
      - show ipv6 route
  register: ipv6_info

- name: Guardar información IPv6
  copy:
    content: |
      === IPv6 Interfaces ===
      {{ ipv6_info.stdout[0] }}

      === IPv6 Routes ===
      {{ ipv6_info.stdout[1] }}
    dest: "evidence/configs/{{ inventory_hostname }}_ipv6_info.txt"
  delegate_to: localhost

- name: Mostrar resumen
  debug:
    msg:
      - "=========================================="
      - "✅ Configuración TEMPORAL aplicada a {{ hostname if hostname is defined else inventory_hostname }}"
      - "⚠️  IMPORTANTE: Cambios NO guardados permanentemente"
      - "⚠️  Router académico compartido - configuración temporal"
      - "✅ IPv6 unicast routing: habilitado temporalmente"
      - "✅ Interfaces IPv6: configuradas temporalmente"
      - "📁 Evidencias guardadas en: evidence/configs/"
      - "=========================================="