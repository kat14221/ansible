# Requirements para Ansible IPv6 Project
# Instalar con: pip3 install -r requirements.txt

# Ansible y collections
ansible>=2.9
ansible-core>=2.9

# Collections (instalar con ansible-galaxy)
# ansible-galaxy collection install cisco.ios
# ansible-galaxy collection install community.vmware

# Bibliotecas Python para Ansible
pyvmomi>=7.0
netaddr>=0.8.0
jmespath>=0.10.0
jinja2>=3.0.0

# Para conexiones SSH a Cisco IOS
ncclient>=0.6.0
paramiko>=2.7.0

# Análisis de red (opcional)
scapy>=2.4.0

# Utilidades
ipython>=7.0.0
